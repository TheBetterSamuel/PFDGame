/// @description Check if contributions are too high

// This condition will be set if the player chooses taxi so that a check later on will take them to a late scene

//if(lateCheck == 1 && lateCheckAfterLunch == 1){
//	scrSlideTransition(TRANS_MODE.GOTO, rAfternoonD2Dialogue2);
//}

//if(lateCheck >= 1){
//	scrSlideTransition(TRANS_MODE.GOTO, rBadEnding1);
//}